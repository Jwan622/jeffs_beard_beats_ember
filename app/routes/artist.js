import Ember from 'ember';

export default Ember.Route.extend({
});
// name of the route and the id that is passed in. That's what mode lis in teh artist.hbs by default so naming is super important of the route
